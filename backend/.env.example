# ============================================
# TraceTree バックエンド環境変数
# ============================================
# 
# 使用方法:
# 1. このファイルを .env にコピー
# 2. 実際の値を設定（特にパスワード）
# 3. .env はGitにコミットしない（.gitignoreで除外済み）
#

# データベース接続URL
# 形式: postgresql+asyncpg://ユーザー名:パスワード@ホスト:ポート/データベース名
# 
# ローカル開発用の例:
DATABASE_URL=postgresql+asyncpg://tracetree:tracetree@localhost:5432/tracetree
#
# 本番環境用の例（強力なパスワードを使用）:
# DATABASE_URL=postgresql+asyncpg://tracetree:STRONG_PASSWORD_HERE@db-server:5432/tracetree_prod

# アプリケーション設定
APP_NAME=TraceTree API
DEBUG=True  # 本番環境では False に設定
